import * as deviceManagerActions from "./features/deviceManager/actionTypes"
import * as webSocketsActions from "./features/websocket/actionTypes"

export const actions = {
    deviceManagerActions,
    webSocketsActions
}
